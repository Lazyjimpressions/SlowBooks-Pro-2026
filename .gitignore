__pycache__/
*.pyc
*.pyo
.env
.env.*
!.env.example
!.env.*.example
*.egg-info/
dist/
build/
*.db
*.sqlite3
*.db-*
*.sqlite
*.sqlite-*
*.sqlite3-*
*.dump
*.backup
.idea/
.vscode/
.codex/
.claude/settings.local.json
AGENTS.local.md
CLAUDE.local.md
*.swp
*.swo
*~
# Phase 9.5 — auto-generated Fernet master key for settings encryption.
# NEVER commit this file; losing it means losing access to every
# encrypted settings value (API keys etc.). Back it up separately.
.slowbooks-master.key

# Session signing key (auto-generated on first boot). NEVER commit.
.slowbooks-session.key

# Pytest / coverage artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.ruff_cache/

# User-uploaded attachments / logos — runtime data, not source
app/static/uploads/

# Private company and financial data. Sanitized fixtures belong only under
# tests/fixtures and must use invented identities and values.
private-data/
local-data/
companies/
imports/
exports/
reports/
*.ofx
*.qfx
*.qbo
*.iif
*.log

# Signing material and credential exports
*.pem
*.p12
*.pfx
*.mobileprovision
credentials*.json
secrets*.json

# pg_dump output — the backups/ dir itself stays tracked (via .gitkeep)
# so the backup feature has somewhere to drop files on a fresh install,
# but the dumps themselves are runtime data.
backups/*
!backups/.gitkeep

# OS-level cruft
.DS_Store
Thumbs.db

# Build artifacts from pip installs done in this directory
*.egg


# Coverage XML artifact from pytest --cov (CI writes its own)
coverage.xml

# Generated by the macOS Actions build from assets/icon-256.png.
packaging/macos/slowbookspro.icns
