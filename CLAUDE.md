# Claude Code entrypoint

Read and follow `AGENTS.md`, then `docs/agent/START_HERE.md` and
`docs/agent/CURRENT_STATE.md` before making changes. Those files are the
cross-platform source of truth. Do not create a second Claude-specific copy of
the project method.
